import type { NextPage } from "next";
import { Layout } from "layout";

const Login: NextPage = () => {
  return (
    <Layout>
      <h1>Hello, Login!</h1>
    </Layout>
  );
};

export default Login;
